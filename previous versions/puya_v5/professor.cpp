#include "professor.h"

professor::professor(string firstname, string lastname, string id, string field, bool status){
			person::firstname = firstname;
			person::lastname = lastname;
			person::id = id;
			professor::field = field;
			professor::status = status;
		}
